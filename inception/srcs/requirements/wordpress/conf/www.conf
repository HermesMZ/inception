[www]

user = www-data
group = www-data

pm = dynamic

listen = 0.0.0.0:9000

pm.max_children = 10
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3

ping.path = /ping
ping.response = pong