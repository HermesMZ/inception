1️⃣ README.md – pour tout le monde (peer, staff, recruteur)

Objectif : synthétique, accessible, introduit le projet, comment le lancer rapidement et où trouver plus d’infos.

Structure recommandée :

Titre + Mention 42

Exemple : This project has been created as part of the 42 curriculum by <login>.

Description

Objectif général du projet

Stack utilisée et services principaux

Points clés techniques (Docker, secrets, réseau, volumes) en résumé

Instructions

Prérequis

Installation / setup initial (git clone, .env, secrets.sh)

Commandes Make principales pour lancer/arrêter les services

Accès aux services

URLs et ports des services principaux (WordPress, Adminer, FTP, Portainer…)

Resources

Documentation officielle, tutoriels

Usage d’IA si applicable

Optionnel : Project Description / Technical Choices

VM vs Docker

Secrets vs ENV

Network Bridge vs Host

Volumes vs Bind Mounts

✅ Court, synthétique, pas de tuto détaillé, pas d’explications internes.

2️⃣ USER_DOC.md – pour l’utilisateur final ou administrateur

Objectif : guide pratique pour démarrer et utiliser l’infrastructure.

Structure recommandée :

Introduction

Bref rappel du projet et de la stack

Services Overview

Liste des services avec fonction et URL/port

Starting / Stopping the project

Commandes Make et explications (up/down/start/stop/re)

Précautions sur les volumes/persistance

Accessing Services

WordPress admin

Adminer / DB

FTP

Portainer

Dashboard statique

Managing Credentials

Emplacement des secrets (srcs/secrets/)

Lecture / modification des mots de passe

Sécurité et permissions (chmod 600)

Verifying Service Health

docker ps ou make status

Logs (make logs)

Tips & Troubleshooting

Redémarrage de services

Vérification des volumes

✅ Pas de détails techniques sur Dockerfile ou scripts internes, juste ce que l’utilisateur doit savoir pour utiliser le stack.

3️⃣ DEV_DOC.md – pour développeur / évaluateur technique

Objectif : montrer que tu comprends Docker, la stack et l’infrastructure, comment construire et maintenir le projet.

Structure recommandée :

Environment Setup

VM / OS / prérequis

Scripts de setup (1-install-vm.sh, 2-install-docker.sh, 3-setup-inception.sh)

Secrets et permissions

.env configuration

Project Structure

Arborescence détaillée des dossiers et fichiers

Différence entre requirements/ et bonus/

Building and Deploying

Dockerfile et Compose usage

Makefile commandes et explications

Ordre de lancement des services et depends_on

Container Management

Volumes vs Bind mounts, healthchecks, secrets

Inspecter containers (docker ps, docker logs, docker exec)

Data Persistence

Quels volumes pour quelles données

Où sont stockés les fichiers sur le host

Service Configuration

Secrets, users, ports, network, bridge

Configuration spécifique des services (MariaDB, WordPress, NGINX)

Bonus Services

Adminer : interface web pour gérer MariaDB (configuration PHP, connexion DB)

Redis : cache object pour WordPress (configuration redis.conf, intégration WP)

FTP : vsftpd pour transfert de fichiers (users, permissions, passive mode)

Portainer : interface de gestion Docker (volumes, init script)

Dashboard : page statique NGINX (présentation des services)

Spécificités techniques de chaque bonus (ports, volumes, dépendances)

Development Workflow

Comment ajouter/modifier un service

Comment rebuild / update l’infra

Debugging / Troubleshooting

Logs, healthchecks, erreurs courantes

Conseils pour réinitialisation (make re)

✅ Très détaillé, explique les choix techniques, montre ta compréhension, mais reste centré sur ce qu’un dev doit savoir pour maintenir ou améliorer le projet.

💡 Règle pratique pour éviter les répétitions :

README : vue d’ensemble, très synthétique.

USER_DOC : tout ce que l’utilisateur doit faire pour démarrer et utiliser la stack.

DEV_DOC : détails techniques, comment ça fonctionne et comment le maintenir.
